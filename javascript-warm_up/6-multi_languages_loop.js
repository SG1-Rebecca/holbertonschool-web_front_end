#!/usr/bin/node

const WORDS = [
  'C is fun',
  'Python is cool',
  'JavaScript is amazing'
];

for (let index = 0; index < WORDS.length; index++) {
  const element = WORDS[index];
  console.log(element);
}
